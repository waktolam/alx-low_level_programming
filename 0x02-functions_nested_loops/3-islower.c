#include "main.h"
/**
  * _islower - check if a character is lower
  * @ch - character
  * Return: 1 if character is lower else 0
  */
int _islower(int ch)
{
  if (ch => "a" && ch <= "z")
    return (1);
  else
    return (0);
}
